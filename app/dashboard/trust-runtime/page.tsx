import { ContinuousTrustDashboard } from "@/src/components/continuous-trust/ContinuousTrustDashboard";
import { continuousTrustRepository } from "@/src/lib/continuous-trust/repository";
import { trustArchitectureUiContext } from "@/src/lib/trust-architecture/ui-context";

export const dynamic = "force-dynamic";
export default async function ContinuousTrustRuntimePage() { const { workspace } = await trustArchitectureUiContext("/dashboard/trust-runtime"); if (!workspace) return <main className="min-h-screen bg-[#04070c] p-8 text-amber-200">Enterprise workspace required.</main>; const repository = continuousTrustRepository(); const generatedAt = new Date().toISOString(); const [runtimeRows, alerts, providers, evidenceRows, assessments] = await Promise.all([repository.listRuntime(workspace.id, 100), repository.alerts(workspace.id, 100), repository.providerHealth(workspace.id, 100), repository.listEvidence(workspace.id, null, 100), repository.recentAssessments(workspace.id, 50)]); return <main className="min-h-screen bg-[#04070c] px-5 py-10 text-white"><div className="mx-auto max-w-7xl"><header className="mb-8"><p className="text-sm uppercase tracking-[0.2em] text-cyan-300">Continuous Trust Runtime</p><h1 className="mt-3 text-4xl font-semibold">Operational trust as conditions change</h1><p className="mt-4 max-w-4xl leading-7 text-zinc-400">Live state, drift, expiring evidence, alerts and provider health derive from tenant-scoped evidence and authoritative Trust State decisions. Missing measurements remain explicit.</p></header><ContinuousTrustDashboard initialRuntime={runtimeRows.slice(0, 100)} initialAlerts={alerts} initialProviders={providers} initialEvidence={evidenceRows.slice(0, 100)} initialAssessments={assessments} generatedAt={generatedAt} /></div></main>; }
