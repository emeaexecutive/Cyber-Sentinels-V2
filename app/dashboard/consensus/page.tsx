import Link from "next/link";
import { ConsensusWorkspace } from "@/src/components/consensus/ConsensusWorkspace";
import { consensusUiContext } from "@/src/lib/consensus/ui-context";
export const dynamic="force-dynamic";
export default async function ConsensusDashboardPage(){const {workspace}=await consensusUiContext("/dashboard/consensus");return <main className="min-h-screen bg-[#04070c] px-5 py-10 text-white"><div className="mx-auto max-w-7xl"><header><p className="text-sm uppercase tracking-[0.2em] text-cyan-300">Provider Consensus Engine™</p><h1 className="mt-3 text-4xl font-semibold">Explainable trust consensus</h1><p className="mt-4 max-w-4xl leading-7 text-zinc-400">Provider count is not truth. Capability, cryptographic verification, freshness, health, independence, policy and contradiction determine every state transition.</p><nav className="mt-5 flex flex-wrap gap-4 text-sm"><Link href="/admin/consensus/providers" className="text-cyan-300 hover:underline">Provider health</Link><Link href="/admin/consensus/policies" className="text-cyan-300 hover:underline">Policy administration</Link></nav></header><div className="mt-8">{workspace?<ConsensusWorkspace enterpriseId={workspace.id} mode="dashboard"/>:<p className="rounded-xl border border-amber-800 p-5 text-amber-200">Enterprise workspace required.</p>}</div></div></main>;}
