import { ConsensusWorkspace } from "@/src/components/consensus/ConsensusWorkspace";
import { consensusUiContext } from "@/src/lib/consensus/ui-context";
export const dynamic="force-dynamic";
export default async function ConsensusPoliciesAdminPage(){const {workspace}=await consensusUiContext("/admin/consensus/policies",true);return <main className="min-h-screen bg-[#04070c] px-5 py-10 text-white"><div className="mx-auto max-w-6xl"><p className="text-sm uppercase tracking-[0.2em] text-cyan-300">Consensus governance</p><h1 className="mt-3 text-4xl font-semibold">Versioned consensus policies</h1><p className="mt-4 max-w-4xl text-zinc-400">Policy changes create immutable versions and canonical Trust Events. Production changes should pass simulation and four-eyes review before activation.</p><div className="mt-8">{workspace?<ConsensusWorkspace enterpriseId={workspace.id} mode="policies"/>:<p className="text-amber-200">Enterprise workspace required.</p>}</div></div></main>;}
