import { architectureContext,architectureCorrelationId,architectureFailure,architectureReference,architectureResponse } from "@/src/lib/trust-architecture/http";
import { trustArchitectureRepository } from "@/src/lib/trust-architecture/repository";
export async function GET(request:Request,context:{params:Promise<{subjectId:string}>}){const correlationId=architectureCorrelationId(request);try{const auth=await architectureContext(request);const subjectId=architectureReference((await context.params).subjectId,"subjectId");const subject=await trustArchitectureRepository().subject(auth.enterpriseId,subjectId);if(!subject)return architectureResponse({ok:false,code:"SUBJECT_NOT_FOUND",error:"Trust subject was not found."},404,correlationId);return architectureResponse({ok:true,...subject},200,correlationId);}catch(error){return architectureFailure(error,correlationId);}}
