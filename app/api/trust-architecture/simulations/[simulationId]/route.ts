import { architectureContext,architectureCorrelationId,architectureFailure,architectureReference,architectureResponse } from "@/src/lib/trust-architecture/http";
import { trustArchitectureRepository } from "@/src/lib/trust-architecture/repository";
export async function GET(request:Request,context:{params:Promise<{simulationId:string}>}){const correlationId=architectureCorrelationId(request);try{const auth=await architectureContext(request);const simulationId=architectureReference((await context.params).simulationId,"simulationId");const simulation=await trustArchitectureRepository().simulation(auth.enterpriseId,simulationId);if(!simulation)return architectureResponse({ok:false,code:"SIMULATION_NOT_FOUND",error:"Simulation was not found."},404,correlationId);return architectureResponse({ok:true,simulation},200,correlationId);}catch(error){return architectureFailure(error,correlationId);}}
