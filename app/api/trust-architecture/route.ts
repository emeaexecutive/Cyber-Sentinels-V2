import { architectureContext,architectureCorrelationId,architectureFailure,architectureResponse } from "@/src/lib/trust-architecture/http";
import { trustArchitectureRepository } from "@/src/lib/trust-architecture/repository";
export async function GET(request:Request){const correlationId=architectureCorrelationId(request);try{const auth=await architectureContext(request);const repository=trustArchitectureRepository();const [domains,overview,health]=await Promise.all([repository.domains(),repository.overview(auth.enterpriseId),repository.health(auth.enterpriseId)]);return architectureResponse({ok:true,architecture:{version:"1.0.0",domains,overview,health}},200,correlationId);}catch(error){return architectureFailure(error,correlationId);}}
