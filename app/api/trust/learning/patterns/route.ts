import { enterpriseTrustLearningRepository } from "@/src/lib/trust-learning/repository";
import { trustLearningContext, trustLearningCorrelationId, trustLearningFailure, trustLearningResponse } from "@/src/lib/trust-learning/http";
export async function GET(request: Request) { const correlationId = trustLearningCorrelationId(request); try { const auth = await trustLearningContext(request); const raw = Number(new URL(request.url).searchParams.get("limit") ?? 100); const limit = Number.isFinite(raw) ? Math.min(200, Math.max(1, raw)) : 100; const patterns = await enterpriseTrustLearningRepository().patterns(auth.enterpriseId, limit); return trustLearningResponse({ ok: true, patterns }, 200, correlationId); } catch (error) { return trustLearningFailure(error, correlationId); } }
