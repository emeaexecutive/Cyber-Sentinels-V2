import { enterpriseTrustLearningRepository } from "@/src/lib/trust-learning/repository";
import { TrustArchitectureApiError, trustLearningContext, trustLearningCorrelationId, trustLearningFailure, trustLearningReference, trustLearningResponse } from "@/src/lib/trust-learning/http";
export async function GET(request: Request, context: { params: Promise<{ patternId: string }> }) { const correlationId = trustLearningCorrelationId(request); try { const auth = await trustLearningContext(request); const patternId = trustLearningReference((await context.params).patternId, "patternId"); const pattern = await enterpriseTrustLearningRepository().pattern(auth.enterpriseId, patternId); if (!pattern) throw new TrustArchitectureApiError("Trust pattern was not found.", 404, "TRUST_PATTERN_NOT_FOUND"); return trustLearningResponse({ ok: true, pattern }, 200, correlationId); } catch (error) { return trustLearningFailure(error, correlationId); } }
