import { architectureContext,architectureCorrelationId,architectureFailure,architectureReference,architectureResponse,assertArchitectureMutation } from "@/src/lib/trust-architecture/http";
import { createTrustPolicy } from "@/src/lib/trust-architecture/service";
export async function PATCH(request:Request,context:{params:Promise<{policyId:string}>}){const correlationId=architectureCorrelationId(request);try{assertArchitectureMutation(request);const auth=await architectureContext(request,["owner","admin"]);const policyId=architectureReference((await context.params).policyId,"policyId");const policy=await createTrustPolicy({enterpriseId:auth.enterpriseId,actorId:auth.user.id,value:await request.json(),correlationId,expectedPolicyId:policyId});return architectureResponse({ok:true,policy},201,correlationId);}catch(error){return architectureFailure(error,correlationId);}}
