# CS-ENG-001 implementation matrix

## Audit basis

This matrix records repository evidence at local `main` commit `9484a95` on 2026-07-18. The scan found 224 App Router pages, 118 API route modules, 58 Supabase migrations and 46 test modules. A name in UI copy or Markdown is not implementation evidence.

Status vocabulary: `IMPLEMENTED`, `PARTIALLY IMPLEMENTED`, `DOCUMENTED ONLY`, `MISSING`, `BLOCKED BY CREDENTIALS`, `BLOCKED BY EXTERNAL CONFIGURATION`.

| Part | Capability | Required files/configuration | Status | Evidence | Gap | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Part 1 | Repository foundation | Next.js, TypeScript, lint and package metadata | IMPLEMENTED | `package.json`, `package-lock.json`, `tsconfig.json`, `eslint.config.mjs`, `next.config.mjs` | None found in repository structure | Keep dependency and framework versions reviewed |
| Part 1 | Package quality scripts | lint, typecheck, test and build scripts | IMPLEMENTED | `package.json` defines `lint`, `typecheck`, `test`, `build` and `validate` | Deployment evidence is separate | Run all gates before every Production push |
| Part 1 | Production Git workflow | `main`, fast-forward pull, reviewed commit and push | PARTIALLY IMPLEMENTED | `docs/engineering/deployment-policy.md`; Vercel project reports Production Branch `main` | Git-host branch protection is not verified; the superseded branch still exists pending safe cleanup | Verify branch protection and remove the old branch only after Production verification |
| Part 1 | Architecture decisions | ADR directory and decision records | DOCUMENTED ONLY | `docs/adr/ADR-001-provider-neutrality.md` through `ADR-010-identity-providers.md` | ADRs describe decisions but do not enforce runtime behavior | Link accepted ADRs to implementing code and tests |
| Part 2 | App Router and layouts | Pages, layouts, loading and error boundaries | IMPLEMENTED | 224 `app/**/page.tsx` modules; `app/layout.tsx`, `app/enterprise/layout.tsx`, `app/error.tsx` | Route-level browser coverage is not exhaustive | Maintain route inventory and browser smoke tests |
| Part 2 | Route protection middleware | User, admin and internal-route authentication gates | IMPLEMENTED | `middleware.ts`; unauthenticated live `/dashboard` and `/back-office` redirect to `/login` with `X-Robots-Tag` | Authenticated and expired-session paths were not exercised live in this audit | Run credentialed auth regression in Production |
| Part 2 | Service and domain layers | Reusable server/domain modules | IMPLEMENTED | `lib/core/`, `lib/runtime/`, `lib/providers/`, `lib/governance/`, `lib/workflows/` | Several overlapping legacy and current engines increase ownership ambiguity | Consolidate canonical entry points per architecture review |
| Part 2 | API surface | App Router route handlers and contracts | IMPLEMENTED | 118 `app/api/**/route.ts` modules; `docs/api/api-overview.md`; `lib/api/public-contracts.ts` | Not every endpoint has deployed contract testing | Add bounded Production smoke coverage for critical endpoints |
| Part 2 | Supabase access layer | Browser/server/admin clients and typed boundaries | PARTIALLY IMPLEMENTED | `lib/supabase/client.ts`, `server.ts`, `admin.ts`, `service-role.ts`, `lib/env.ts` | Production schema, grants and migration application were not introspected | Validate generated types and applied migration history with safe Supabase credentials |
| Part 2 | Database schema and migrations | Ordered SQL migrations and RLS | PARTIALLY IMPLEMENTED | 58 files under `supabase/migrations/`; schema map in `docs/database/schema-map.md` | Repository presence does not prove Production application or ordering health | Run non-destructive migration status and RLS checks against the linked project |
| Part 2 | Authentication | Supabase session, callback, email verification and login | PARTIALLY IMPLEMENTED | `app/login/page.tsx`, `app/auth/callback/route.ts`, `app/verify-email/page.tsx`, `middleware.ts`, `lib/supabase/server.ts` | Live login UI returns 200, but sign-in and session persistence require test credentials | Complete credentialed Production login/logout/session test |
| Part 2 | Authorization | User/admin separation and admin allowlist | PARTIALLY IMPLEMENTED | `middleware.ts`, `lib/admin-auth.ts`, `lib/auth/isAdmin.ts`; Back Office clears admin cookie when unauthenticated | Admin allowlist is environment-driven and is not a general tenant-role model | Verify allowlist configuration and add durable role/tenant authorization |
| Part 2 | Provider abstraction | Registry, lifecycle, errors and orchestration | IMPLEMENTED | `lib/providers/types.ts`, `registry.ts`, `provider-orchestrator.ts`, adapters and provider test suites | Operational readiness still depends on external provider configuration | Keep contract tests and provider health evidence current |
| Part 2 | Hopae production connectivity | Client credentials, webhook secret and Production base URL | BLOCKED BY CREDENTIALS | `lib/providers/adapters/hopae/`; `tests/hopae-provider-integration.test.mjs`; `scripts/hopae-deployment-readiness.mjs` | This audit did not use Hopae credentials or make a live provider transaction | Run the existing readiness and sandbox workflow with approved secrets |
| Part 3 | Evidence normalization | Canonical evidence mapping and validation | IMPLEMENTED | `lib/core/evidence-normalizer.ts`, `lib/providers/evidence-normalizer.ts`, Hopae normalizer and provider tests | Provider coverage varies by adapter | Add fixtures and schema/version checks for every enabled adapter |
| Part 3 | Evidence Graph | Nodes, relationships, graph queries and UI/API | PARTIALLY IMPLEMENTED | `lib/evidence-graph/evidence-graph.ts`, `query.ts`, `app/api/evidence-graph/route.ts`, graph pages, graph migrations | Some projections and correlation behavior are process-derived rather than a single canonical durable graph | Define canonical persistence and replayable graph-version contract |
| Part 3 | Replay | Replay engine, writer, API and replay pages | PARTIALLY IMPLEMENTED | `lib/core/replay-engine.ts`, `lib/replay/replay-writer.ts`, `app/api/replay/[id]/route.ts`, `app/replay/[id]/page.tsx` | Exact version-pinned deterministic replay is not demonstrated for every input | Persist engine/policy/provider versions and add golden replay tests |
| Part 3 | Trust Memory | Durable memory/evolution modules and lifecycle | PARTIALLY IMPLEMENTED | `lib/trust-memory/trust-memory.ts`, `trust-evolution.ts`, Trust Memory APIs/pages and migrations | Retention, deletion and snapshot behavior are not fully verified in Production | Run privacy/retention tests against applied schema |
| Part 3 | Operational Risk Intelligence | Feature pipeline, shadow inference and validation | PARTIALLY IMPLEMENTED | `lib/operational-risk/`, migration `202607170001_operational_risk_intelligence_shadow.sql`, ORI and ML validation tests | Repository labels the model as shadow/readiness behavior; reviewed real-world evidence gate remains | Keep non-authoritative mode until model and dataset acceptance criteria pass |
| Part 3 | Policy evaluation | Policy templates, evaluation and API | IMPLEMENTED | `lib/policy-engine.ts`, `lib/trust-engine/policyEngine.ts`, `app/api/policies/route.ts` | Multiple policy modules require a canonical ownership decision | Route all decisions through one versioned policy contract |
| Part 3 | Trust Decision Engine | Decision engine, execution routes and decision UI | PARTIALLY IMPLEMENTED | `lib/trust/decision-engine.ts`, `lib/core/decision-intelligence.ts`, `/api/trust/decision`, `/api/trust/execute`, `/decision-engine` | Multiple engines and demo/readiness paths prevent proof of one canonical end-to-end TDE | Consolidate entry point and add durable decision-envelope tests |
| Part 3 | Enterprise Trust Reports | Report API, views and exports | PARTIALLY IMPLEMENTED | `app/api/trust-reports/route.ts`, report pages/components, audit-log writes | Product review records CSV as not implemented; export formats and deployed data paths are incomplete | Finish formats and verify authenticated Production export |
| Part 3 | Observability | Health, validation logs and durable telemetry | PARTIALLY IMPLEMENTED | `lib/core/platform-health.ts`, `lib/performance/durable-telemetry.ts`, `runtime_validation_logs` migration, Vercel logs | Source explicitly states process telemetry is not Production APM | Add centralized alerting, retention and service-level dashboards |
| Part 4 | Public website | Homepage, trust, product, legal and enterprise routes | IMPLEMENTED | Public `app/**/page.tsx` surface; homepage and enterprise access returned 200 on the canonical domain | Full link/asset/accessibility traversal was not available in this session | Run browser crawl, keyboard and responsive checks |
| Part 4 | Enterprise journey | Enterprise landing, pilot, buyer documentation and access intake | IMPLEMENTED | `app/enterprise/`, `app/enterprise-access/`, `components/enterprise-*`, enterprise experience tests | Submitted intake was not tested because it would create Production data | Test in an approved controlled account/workflow |
| Part 4 | Dashboard | Protected operational views | IMPLEMENTED | `app/dashboard/` pages and dashboard components; live unauthenticated protection verified | Authenticated Supabase rendering was not tested live | Run credentialed dashboard smoke test |
| Part 4 | Governance | Governance queues, policy and access-governance views | IMPLEMENTED | `app/governance`, `app/dashboard/governance`, `lib/governance/`, `lib/operational-governance/` | Real reviewer workflows and durable state were not exercised | Execute a controlled end-to-end governance case |
| Part 4 | Back Office | Admin surface and operational queues | PARTIALLY IMPLEMENTED | `app/back-office/page.tsx`, `app/admin/`, admin middleware gates | Admin environment configuration and live authorized rendering are unverified | Verify `ADMIN_EMAILS` and admin access using an approved account |
| Part 4 | Reports | Trust, hiring, interview, receipt and compliance surfaces | PARTIALLY IMPLEMENTED | `app/trust/*report*`, `app/compliance-export`, receipt APIs/components | Export completeness and production data quality remain unverified | Add format-specific tests and Production sample evidence |
| Part 4 | Accessibility | Accessibility page and documented review | PARTIALLY IMPLEMENTED | `app/accessibility/page.tsx`, `docs/product/accessibility.md` | No fresh keyboard, screen-reader, contrast, zoom or responsive pass in this session | Complete a manual WCAG review with browser tooling |
| Part 4 | Analytics | Consent-aware product analytics | MISSING | `docs/product/analytics.md` explicitly records analytics as not implemented | No approved runtime analytics/consent implementation | Select a privacy model, implement consent and validate event governance |
| Part 5 | Authentication security | Session validation, verified email, safe redirects | PARTIALLY IMPLEMENTED | `middleware.ts`, auth callback/logout/session routes, auth error handling | Credentialed Production regression and MFA coverage are incomplete | Test login, logout, expiry, refresh and MFA paths |
| Part 5 | Tenancy and RLS | Owner/tenant-scoped database access | PARTIALLY IMPLEMENTED | `202607010001_production_owner_scoped_rls.sql`, other RLS migrations and denial tests | Owner email scoping is not a complete organization/tenant authorization model; applied Production state unverified | Introduce tenant IDs/roles where required and run live denial tests safely |
| Part 5 | Secrets and environment | Server-only secrets and Vercel Production variables | BLOCKED BY CREDENTIALS | `lib/env.ts`, provider config modules, Vercel linked project | CLI inventory did not provide a complete named Production-variable set; values were intentionally not read | Verify names/scopes in Vercel and rotate any stale credentials |
| Part 5 | Webhook integrity | Signature, timestamp, duplicate and replay controls | IMPLEMENTED | Stripe webhook route, ATS webhook security, Hopae callback security, `lib/webhooks/event-ledger.ts`, security tests | Provider-specific live delivery was not replay-tested here | Run signed test events in approved non-destructive workflows |
| Part 5 | Cloudflare edge controls | DNS/proxy, TLS, WAF, bot controls and indexing | BLOCKED BY EXTERNAL CONFIGURATION | Live responses are served by Cloudflare and include `CF-RAY`; CSP allows Turnstile | DNS/WAF rules, bot mode and account settings cannot be proven from the repository or live headers alone | Verify Cloudflare dashboard settings and export an evidence snapshot |
| Part 5 | Turnstile | Client widget and server verification | PARTIALLY IMPLEMENTED | `lib/bot-protection.ts`, `components/turnstile-field.tsx`, auth/waitlist/access routes and tests | Production site/secret key configuration was not verified | Confirm both Production keys and run a controlled valid/invalid token test |
| Part 5 | Rate limiting | Public and sensitive endpoint throttling | PARTIALLY IMPLEMENTED | `lib/security.ts`, `lib/bot-protection.ts` and multiple guarded routes return 429 | Buckets are process-local and do not provide durable distributed enforcement; some endpoints record future work | Move critical limits to a shared edge/durable store and cover all public write routes |
| Part 5 | Security headers | CSP, HSTS, framing, referrer and permissions policy | IMPLEMENTED | `next.config.mjs`; headers verified live on `https://www.cybersentinels.com` | CSP still permits `unsafe-inline` and `unsafe-eval` | Adopt nonces/hashes and remove unsafe directives when compatible |
| Part 5 | Logging and audit | Audit tables, runtime validation and Production logs | PARTIALLY IMPLEMENTED | `audit_logs`, `runtime_validation_logs`, provider telemetry, Vercel Production log query | Logging is fragmented and may be process-local; no centralized incident alert proof | Define structured fields, redaction, retention and alert ownership |
| Part 5 | Recovery and rollback | Trust recovery plus release rollback | PARTIALLY IMPLEMENTED | `/api/trust-recovery`, recovery engine, migration recovery comments, deployment policy rollback | Production rollback drill and authenticated recovery flow were not executed | Schedule controlled restore/revert drills and record RTO/RPO evidence |
| Part 5 | Dependency audit | Production dependency advisory gate | IMPLEMENTED | `package.json` pins PostCSS `8.5.14` and uses an npm override for Next's nested copy; `npm audit --omit=dev` reported zero vulnerabilities | The override should be removed when the pinned Next release bundles a fixed PostCSS version | Recheck the dependency tree on each Next upgrade |
| Part 5 | Automated testing | Lint, types, unit/integration/RLS/load/build gates | IMPLEMENTED | 46 tests; extensive package scripts; deployed security and load harnesses | Some live/provider/RLS tests require credentials and are intentionally outside the default test chain | Run credentialed suites only in approved safe environments |
