# Epic 15 production-evidence demo

1. Show the strict schema and 30 pending fixtures — **Test**.
2. Open protected dual review — **Live source control; Human Review Required**.
3. Show approved-only metrics — **Calibration Incomplete**.
4. Run Hopae readiness — **Awaiting Credentials** unless target configured.
5. Show provider execution record contract — **Deployment Required**.
6. Show signed callback and idempotency tests — **Test**.
7. Show Replay, Graph, Memory and Pack linkage — **Simulated/Test** until real flow.
8. Show provider-neutral webhook ledger — **Migration Required** until applied.
9. Show deployed security harness — **Blocked** unless explicitly run.
10. Show RLS denial harness — **Blocked** unless target supplied.
11. Show durable stage schema and retention — **Migration Required**.
12. Show load harness and percentile gates — **Pilot Traffic Required**.
13. Close on four evidence-linked blocker cards — **SOURCE READY — DEPLOYMENT EVIDENCE REQUIRED**.
