# Trust Graph events

Every entity, evidence, relationship, and provider mutation emits an immutable versioned event in the same database transaction. Events carry actor and correlation attribution but no raw provider payload.
