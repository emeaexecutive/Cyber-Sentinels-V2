# Providers

Defines the provider-neutral `TrustProvider` contract for verification, health, confidence, cost, and latency. Adapters must normalize responses and fail closed before evidence ingestion.
