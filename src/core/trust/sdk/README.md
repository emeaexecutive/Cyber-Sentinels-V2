# SDK contracts

Defines transport-neutral client, webhook, GraphQL-ready node, and OpenAPI operation contracts. This sprint does not ship a generated client or GraphQL server; consumers can implement these stable interfaces over the authenticated REST APIs.
