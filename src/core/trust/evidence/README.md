# Evidence

Defines tenant-bound evidence nodes, relationships, validation, safe metadata, and the persistence contract. Raw provider payloads and secrets are deliberately excluded. The module does not make authorization decisions.
