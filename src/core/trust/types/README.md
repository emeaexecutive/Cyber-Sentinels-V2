# Trust Graph types

Defines the versioned, transport-neutral entity, evidence, relationship, source, timeline, graph, summary, and statistics contracts. All records carry a tenant identifier; metadata is restricted to scalar values.
