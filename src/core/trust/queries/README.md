# Trust Graph queries

Provides reusable, bounded tenant queries for shared device/email fingerprints, identity evidence, provider failures, linked AI agents, and orphan entities. Email and device matching accepts hashes only; raw identifiers are not graph query keys.
