# Trust DNA

The EPIC 22 engine projects tenant-scoped Enterprise Trust Graph evidence into twelve weighted, explained dimensions. `calculate()` and `recalculate()` produce immutable `trust-dna-v2` profiles; the legacy `build()` path remains compatible with EPIC 20 Trust Intelligence callers.

Scores are context for accountable decisions. They do not authenticate identities, authorize actions or bypass RLS.
