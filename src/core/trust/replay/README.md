# Replay

EPIC 23 reconstructs immutable, tenant-scoped forensic timelines for Enterprise Trust Graph entities. Events are chained, searchable, explainable and exportable. Legacy EPIC 20 identity Replay remains compatible and is explicitly reported as pre-chain history.

Replay retains normalized domain events, not raw application logs or provider payloads.
