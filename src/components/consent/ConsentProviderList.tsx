import type { ConsentCategoryDefinition } from "@/src/lib/consent/types";
export function ConsentProviderList({ categories }: { categories: ConsentCategoryDefinition[] }) { return <div className="grid gap-3 md:grid-cols-2">{categories.map((category) => <article key={category.key} className="rounded-xl border border-zinc-800 bg-zinc-950 p-5"><h2 className="font-semibold text-white">{category.name}</h2><p className="mt-2 text-sm text-zinc-400">{category.providers.join(", ")}</p><p className="mt-2 text-xs text-zinc-500">{category.storageItems.join(" · ")} · {category.retention}</p></article>)}</div>; }
