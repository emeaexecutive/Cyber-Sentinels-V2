import type { ConsensusPolicy } from "@/src/lib/consensus/types";
export function PolicySummary({policy}:{policy:ConsensusPolicy}){return <article className="rounded-xl border border-zinc-800 bg-zinc-950 p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{policy.name}</h2><p className="mt-1 text-xs text-zinc-500">{policy.policyId}@{policy.version}</p></div><span className="rounded-full border border-zinc-700 px-2 py-1 text-xs">{policy.active?"ACTIVE":"INACTIVE"}</span></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-zinc-500">Verified</dt><dd>{policy.verifiedThreshold}/100 · {policy.minimumIndependentGroupsVerified} groups</dd></div><div><dt className="text-zinc-500">Trusted</dt><dd>{policy.trustedThreshold}/100 · {policy.minimumIndependentGroupsTrusted} group</dd></div><div><dt className="text-zinc-500">Material conflict</dt><dd>{policy.materialConflictOutcome}</dd></div><div><dt className="text-zinc-500">Critical revocation</dt><dd>{policy.criticalRevocationOutcome}</dd></div></dl><p className="mt-4 text-xs text-zinc-500">Mandatory signals: {policy.mandatorySignals.join(", ")}</p></article>;}
