export function ConsensusScore({confidence}:{confidence:number}){const score=Math.max(0,Math.min(100,Math.round(confidence)));return <div className="grid gap-2" aria-label={`Consensus confidence ${score} out of 100`}><div className="flex items-end justify-between"><span className="text-sm text-zinc-400">Deterministic confidence</span><strong className="text-3xl">{score}<span className="text-base text-zinc-500">/100</span></strong></div><div className="h-2 overflow-hidden rounded-full bg-zinc-800"><div className="h-full rounded-full bg-cyan-400" style={{width:`${score}%`}} /></div><p className="text-xs text-zinc-500">A reproducible policy score—not probability or certainty.</p></div>;}
