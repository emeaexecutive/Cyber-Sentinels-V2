type Row={providerKey:string;displayName:string;capabilityState:string;positiveEvidence:boolean;baseWeight:number;health:{state:string;reasonCodes?:string[]};reasonCodes:string[]};
export function ProviderHealthGrid({providers}:{providers:Row[]}){return <section aria-labelledby="health-grid-title"><h2 id="health-grid-title" className="text-2xl font-semibold">Provider capability and health</h2><div className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{providers.map((provider)=>{const world=provider.providerKey==="world_id";return <article key={provider.providerKey} className="rounded-xl border border-zinc-800 bg-zinc-950 p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold capitalize">{provider.displayName}</h3><p className="text-xs text-zinc-500">{provider.providerKey}</p></div><span className="rounded-full border border-zinc-700 px-2 py-1 text-xs">{provider.health.state}</span></div><dl className="mt-4 grid gap-1 text-sm text-zinc-300"><div>Capability: {provider.capabilityState}</div><div>Positive weight: {provider.baseWeight.toFixed(2)}</div><div>Contribution: {provider.positiveEvidence?"Eligible when evidence passes":"Zero"}</div></dl>{world?<div className="mt-4 rounded-lg border border-amber-800 bg-amber-950/20 p-3 text-sm text-amber-100"><strong>INCONCLUSIVE</strong><p>Server verification pending</p><p>Zero positive consensus contribution</p></div>:null}<ul className="mt-4 grid gap-1 text-xs text-zinc-500">{[...new Set([...provider.reasonCodes,...(provider.health.reasonCodes??[])])].map((code)=><li key={code}>{code}</li>)}</ul></article>;})}</div></section>;}
