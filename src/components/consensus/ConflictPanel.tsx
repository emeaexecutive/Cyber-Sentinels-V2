import type { ConsensusConflict } from "@/src/lib/consensus/types";
export function ConflictPanel({conflicts}:{conflicts:ConsensusConflict[]}){return <section aria-labelledby="conflict-title" className="rounded-xl border border-zinc-800 bg-zinc-950 p-5"><h2 id="conflict-title" className="text-xl font-semibold">Conflict analysis</h2>{conflicts.length?<ul className="mt-4 grid gap-3">{conflicts.map((item)=><li key={item.conflictId} className="rounded-lg border border-amber-900 bg-amber-950/20 p-4"><strong>{item.severity} · {item.type}</strong><p className="mt-1 text-sm text-zinc-300">{item.explanation}</p><code className="mt-2 block text-xs text-amber-200">{item.reasonCode}</code></li>)}</ul>:<p className="mt-3 text-sm text-zinc-400">No resolved evidence contradiction was detected.</p>}</section>;}
